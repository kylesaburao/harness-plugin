# Optional fallbacks for wake-desktop.js, used only when the matching flag is absent.
# Prefer passing --mac, --ip, and --timeout directly.
# Load with: node --env-file=.env scripts/wake-desktop.js
MAC_ADDRESS="TODO"
IP_ADDRESS="TODO"
TIMEOUT=120
